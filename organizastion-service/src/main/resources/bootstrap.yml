spring:
  application:
    name: organizationservice #must map config server folder name
  profiles:
    active: prod
  cloud:
    config:
      enabled: true
      uri: http://localhost:8888

management:
  security:
    enabled: false

logging:
  file: /tmp/server.log