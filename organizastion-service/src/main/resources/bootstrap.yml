spring:
  application:
    name: organizationservice #must map config server folder name
  profiles:
    active: dev
  cloud:
    config:
      enabled: true
      uri: http://localhost:8888

management:
  security:
    enabled: false