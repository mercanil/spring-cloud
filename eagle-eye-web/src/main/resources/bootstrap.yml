spring:
  application:
    name: licensingservice #must map config server folder name
  profiles:
    active: dev
  cloud:
    config:
      uri: http://localhost:8888

management:
  security:
    enabled: false